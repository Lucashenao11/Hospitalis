/**
 * CAPA DE DOMINIO — Enumeraciones
 *
 * Los enums representan valores controlados del negocio.
 * No dependen de ninguna librería externa — son pura lógica de dominio.
 */
export enum Role {
  ADMIN  = 'admin',
  MEDICO = 'medico',
}
